Create a C++ class named television that has data members to hold the model number and the screen size in inches, and the price. Member function include overloaded insertion and extraction operators. if more then four digits are entered for the model, if the screen size is smaller then 12 or greater then 70 inches, or if the price is negative or over $5000 then throw an integer. Write a main() function that instantiates a television object, allows user to enter data and displays the data members. if an exception is caught, replace all the data member values with zero values.

Pls. follow the Coding insturction given below URL:

https://topgear-app.wipro.com/c-programming-0/content/c-programming-codi...